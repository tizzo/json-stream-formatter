{
  "name": "json-stream-formatter",
  "version": "1.0.1",
  "description": "A command line tool and through stream for formatting streams of JSON objects into a text stream.",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "watch": "mocha --watch",
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha"
  },
  "bin": {
    "json-stream-formatter": "./bin/json-stream-format"
  },
  "author": "Howard Tyson",
  "license": "GPL2+",
  "repository": "https://github.com/tizzo/json-stream-formatter.git",
  "dependencies": {
    "chalk-twig-filters": "0.0.2",
    "split2": "^2.0.0",
    "through2": "^2.0.0",
    "twig": "^0.7.2",
    "yargs": "^1.3.1"
  },
  "devDependencies": {
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.2",
    "mocha": "^1.21.4",
    "should": "^4.0.4"
  }
}
